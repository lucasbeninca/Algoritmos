algoritmo "multiplicacao"
var
multiplicador, multiplicando : inteiro
produto : real

inicio
escreval("escreva o multiplicador ")
leia(multiplicador)

escreval("escreva  multiplicando ")
leia(multiplicando)

produto <- (multiplicador * multiplicando)
escreval("o produto da multiplicação dos dois numeros e: ", produto)

fimalgoritmo